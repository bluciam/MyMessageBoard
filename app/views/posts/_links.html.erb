
<%- if controller_name == "show" && (can? :edit, @post) %>
  <%= link_to "Edit post", edit_post_path(@post.id), class: "btn
  btn-primary " %>
<% end %>

<% if can? :update, @post %>
  <%= link_to "Delete post", post_path(@post.id), method: :delete,
  class: "btn btn-default "%>
<% end %>

  <%= link_to "All posts", posts_path, class: "btn btn-default " %>

<% if can? :new, Post %>
  <%= link_to "Add new post", new_post_path, class: "btn btn-default " %>
<% end %>


